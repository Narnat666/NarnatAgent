# T2.5b 修正任务：llm 积木消除两处数据双轨

## 背景（先读再动手）

T2.5（llm 积木）完成报告披露两处"重复数据"（架构审计确认为双轨，须消除）：

1. **SYNTHETIC_THINKING 重复**：`v2/narnat_agent/contracts/messages.py` 已定义该共享字面量（messages 积木已引用），但 `v2/narnat_agent/llm/anthropic_backend.py` 内置了一份同文本常量。contracts 是 L0，llm（L1）可直接 import——不存在同层障碍。
2. **thinking 映射表重复**：`v2/narnat_agent/config/defaults.py` 持有权威表（T2.1 交付，已通过 defaults 基准对照），`v2/narnat_agent/llm/thinking.py` 内置了一份"逐字搬运"的副本（为独立可用而设）。唯一权威原则下，副本应删除。

**约束（design D1/D14 同源）**：行为等价不变；分层不变；测试可 `from narnat_agent.config import ...`（tests 不受包内分层约束）。

## 修改要求

1. `v2/narnat_agent/llm/anthropic_backend.py`：删除内置 `SYNTHETIC_THINKING`，改为 `from ..contracts.messages import SYNTHETIC_THINKING`（保持本模块的对外导出面不变）。
2. thinking 映射表：**删除 llm 内置副本**；将表（或解析函数）改为**必需注入**——构造 `LLMClient` 时传入（形参名自定，如 `thinking_resolver` / `thinking_rules`），无默认值；未传即报错（TypeError）而非静默用副本。
   - 注入物为 `config.defaults` 提供的能力（如 `resolve_thinking_params` / `resolve_thinking_passback` 或表对象——以现有实现最贴合者为准，保持调用语义不变）。
   - `v2/narnat_agent/llm/thinking.py` 若只剩空壳则删除；若仍有非表逻辑（如参数装配），保留但不得含映射表数据。
3. 测试调整（`v2/tests/unit/test_llm.py`）：构造处注入 `config.defaults` 的能力；删除"防漂移哨兵"类测试（副本已不存在）；保持 233 例中其余用例全绿（等价断言不放松）。
4. 装配接线（T4.3 的预埋）：在 `llm/__init__.py` 或 client docstring 注明"装配时注入 config.defaults 的解析函数"。

## 验收标准（可计算）

1. `cd /d D:\desktop\NarnatAgent\v2 && python -m pytest tests/unit/test_llm.py -q` 全绿（用例数与调整前持平或说明增减）
2. `python tests/check_layering.py` 对 llm 无违规
3. 源头唯一证据：`grep -rn "output_config\|reasoning_effort\|thinking_budget" v2/narnat_agent/llm/` 命中数 = 0 或仅为 docstring（报告给出命令与结果）
4. `grep -rn "用户中断了工具执行" v2/narnat_agent/` 仅命中 `contracts/messages.py` 一处（报告给出命令与结果）
5. 未传 thinking 注入时构造抛错（报告给出演示）

## 边界条款

- 只改 `v2/narnat_agent/llm/` 与 `v2/tests/unit/test_llm.py`；不改其他文件
- 不运行 narnat；不执行 git 写操作

## 失败报告格式

①已尝试方案；②报错原文；③怀疑原因。「确认失败」是合法终点。
